package com.example.agilelifemanagement.ui.screens.task.stubs

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

/**
 * Stub implementation of the TaskDetailScreen to avoid naming conflicts.
 * The main implementation is in TaskDetailScreen.kt.
 */
@Composable
fun TaskDetailScreen(navController: NavController, taskId: String) {
    // This is just a stub to avoid naming conflicts
    // The real implementation is in the main package
}
