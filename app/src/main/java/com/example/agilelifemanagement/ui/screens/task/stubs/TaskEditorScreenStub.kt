package com.example.agilelifemanagement.ui.screens.task.stubs

import androidx.compose.runtime.Composable
import androidx.navigation.NavController

/**
 * Stub implementation of the TaskEditorScreen to avoid naming conflicts.
 * The main implementation is in TaskEditorScreen.kt.
 */
@Composable
fun TaskEditorScreen(navController: NavController, taskId: String? = null) {
    // This is just a stub to avoid naming conflicts
    // The real implementation is in the main package
}
