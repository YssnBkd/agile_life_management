package com.example.agilelifemanagement.data.local.dao

import androidx.room.Dao

/**
 * Room DAO for managing task dependencies (tasks that depend on other tasks).
 * This is a stub implementation to satisfy the DI requirements.
 */
@Dao
interface TaskDependencyDao {
    // This interface will be implemented when adding task dependency functionality
}
